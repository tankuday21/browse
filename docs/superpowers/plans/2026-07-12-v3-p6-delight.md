# V3 Phase 6 — Delight + Improve Pass — Implementation Plan (compact, build-first)

> Build-first mode per owner: write unit tests but do NOT run suites; verify with assembleDebug + compileDebugUnitTestKotlin only. The consolidated v3 test pass follows immediately after this phase.

**Goal:** K1 asteroid game on the offline error page, the improve pass over ledger minors, menu reorganization for the ~12 new entries. (Release itself — regression, versioning, signed APK — happens in the consolidated pass, not here.)

**Branch:** `feature/v3-p6-delight` off main; merge --no-ff; tag v3-phase-6. No DB migration.

## K1 — Asteroid game ("Lost in space")
- Current error page: find how onPageError renders (VM state → BrowserScreen error view). Add "Tap to play" entry point on the error page ONLY when the error is connectivity-class (ERR_INTERNET_DISCONNECTED / UnknownHost-style descriptions).
- ui/game/AsteroidGame.kt: Compose Canvas game. Starship at bottom, drag/tap-halves to steer; asteroids fall with increasing speed/density; collision ends run; score = seconds survived ×10; high score in DataStore (`asteroidHighScore: Flow<Int>`). Frame loop via withFrameNanos in LaunchedEffect; game state in a plain GameState class. Pure logic module browser/AsteroidPhysics.kt (spawn cadence, collision AABB/circle check, difficulty curve) + unit test (collision math, difficulty monotonic, spawn bounds).
- Orbit styling: starfield background (reuse home aesthetic), ship = simple vector triangle/glow, asteroids = irregular circles. Pause on lifecycle stop; back exits to error page. High-score chip + "again" button on game-over.

## Improve pass (ledger minors from P1–P5, fix what's cheap and visible)
1. Switcher list rows too tall → tighten padding/height.
2. Reopened-tab missing thumbnail → capture on first paint after reopen (or placeholder tint).
3. Bulk-share from selection includes incognito tab URLs → exclude incognito tabs from share payload (privacy).
4. Empty group created from incognito-only selection → guard (no group without persistable tabs).
5. Add-to-group submenu should show current group checkmark.
6. Group header: whole row tappable for collapse (not just chevron).
7. WebViewHolder.close() doesn't clear customView if that tab owned fullscreen → exitFullscreen on close of owning tab.
8. onShowCustomView without active-tab check → only honor for active tab.
9. Menu reorganization (research consensus): top icon action row (Back/Forward/Refresh/Share/Add-to-home) + grouped list: [New tab, New incognito] · [Bookmarks, History, Downloads, Reading list] · [Save for later, Reader mode, Listen?, Find in page, Translate?—no] · [Desktop site, Site settings, Text size?, Print] · [Settings]. Keep incognito-conditional items correct. Unread badge preserved.
10. Sweep: any TODO/stray log lines introduced in P3-P5; consistent icon set on all menu items.

Commits: `feat(v3-p6): asteroid game on offline error page (K1)`, `fix(v3-p6): improve pass - switcher, fullscreen, incognito share guards`, `feat(v3-p6): menu reorganization`.
