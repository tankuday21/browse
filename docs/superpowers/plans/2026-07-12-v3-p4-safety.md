# V3 Phase 4 — Safety & Annoyance Shield — Implementation Plan (compact, build-first)

> Build-first mode per owner: write unit tests but do NOT run suites; verify with assembleDebug + compileDebugUnitTestKotlin only. All testing happens in the consolidated v3 pass.

**Goal:** D1 Safe Browsing with our own interstitial, D2 cookie-banner auto-dismiss, D5 GPC, C3 privacy stats on home.

**Branch:** `feature/v3-p4-safety` off main; merge --no-ff; tag v3-phase-4. No DB migration in this phase.

## D1 — Safe Browsing (WebViewHolder + Compose interstitial + toggle)
- `WebSettingsCompat.setSafeBrowsingEnabled` per WebView when `WebViewFeature.SAFE_BROWSING_ENABLE` supported; driven by new `SettingsRepository.safeBrowsing: Flow<Boolean>` default true (applied like javaScriptEnabled via applyPolicy).
- Override `onSafeBrowsingHit(view, request, threatType, callback)` in the WebViewClient (framework API 27+; guard with Build.VERSION or use WebViewClientCompat pattern consistent with codebase — androidx.webkit already a dep). Only act on main-frame hits (`request.isForMainFrame`); sub-resource hits → `callback.backToSafety(false)` silently.
- Listener event `onSafeBrowsingHit(tabId, url, threatLabel)` → VM state `safeBrowsingWarning` → full-screen Orbit-styled warning composable in BrowserScreen (replaces web content area): shield icon, "This site may be dangerous", threat label ("Phishing"/"Malware"/"Unwanted software"/"Billing"), the url, big filled **Go back** button, small text button "Proceed anyway (not recommended)". Go back → callback.backToSafety(false) + VM clears; Proceed → callback.proceed(false) + clear. Hold the callback in WebViewHolder keyed by tabId (clear on nav/close; guard double-invoke).
- Settings > Privacy section: "Safe Browsing" switch, default ON, subtitle "Warns you before phishing and malware sites (Google Safe Browsing)".

## D2 — Cookie-banner auto-dismiss (annoyance list)
- Fetch snapshot of Fanboy Cookie Monster list (https://secure.fanboy.co.nz/fanboy-cookiemonster.txt) into `app/src/main/assets/adblock/annoyance-cookies.txt` at build-authoring time (commit the snapshot; ~300 KB ok). If fetch fails, fall back to a hand-written subset of ~40 high-value generic cosmetic rules (#onetrust-banner-sdk, #CybotCookiebotDialog, .cc-window, #didomi-host, .qc-cmp2-container, .fc-consent-root, .cmp-banner, [id*="cookie-banner"], etc.).
- Load through existing FilterListParser into a SECOND AdBlockEngine instance (`annoyanceEngine`) in BrowseApplication (network blocking + cosmetic injection both ride existing code paths in WebViewHolder — apply cosmetics from both engines; gate with setting).
- `SettingsRepository.dismissCookieBanners: Flow<Boolean>` default true; Settings > Privacy: "Auto-dismiss cookie banners" switch, subtitle "Hides consent pop-ups (may affect some sites)".

## D5 — Global Privacy Control
- `SettingsRepository.gpcEnabled: Flow<Boolean>` default false (spec: toggle; GPC is a legal signal — user opts in). Settings > Privacy: "Global Privacy Control" switch + one-line explainer.
- JS shim: `WebViewCompat.addDocumentStartJavaScript` (feature-guard DOCUMENT_START_SCRIPT; fallback: evaluateJavascript at onPageStarted) setting `Object.defineProperty(Navigator.prototype,'globalPrivacyControl',{get:()=>true})`, allowedOriginRules ["*"], only when enabled (holder needs enable/disable — simplest: register shim per WebView at obtain() when enabled; toggling takes effect for new tabs, note in subtitle "takes effect on new tabs").
- Header: our own navigations that go through loadUrl gain `Sec-GPC: 1` extra header when enabled (WebViewHolder loadUrl path + shouldOverrideUrlLoading re-dispatch if that's how navigation flows — keep minimal, main-frame only, documented honest limit).

## C3 — Privacy stats on home
- Lifetime counters in DataStore: `blockedCount: Flow<Long>`, incremented batched from the existing onRequestBlocked signal path (VM already receives block events for the per-page counter — find it; accumulate in VM and flush increments to DataStore with a 2s debounce or on 50-block batches to avoid write storms; also flush in onCleared).
- HomePage block under the shortcut grid: shield icon, "N ads & trackers blocked", "~X MB saved (estimated)" where X = N*50KB formatted MB/GB; hide entirely at N==0. Pure helper `PrivacyStatsFormat.format(n): Pair<String,String>` with unit test.

## Files
SettingsRepository (+4 settings), SettingsScreen (Privacy section additions), WebViewHolder (safe browsing + GPC shim + annoyance cosmetics), BrowseApplication (annoyanceEngine load), BrowserViewModel (warning state, stats accumulator), BrowserScreen (interstitial composable), HomePage (stats block), browser/PrivacyStatsFormat.kt (new, pure+test), assets/adblock/annoyance-cookies.txt (new snapshot).

Commits: `feat(v3-p4): safe browsing interstitial + toggle (D1)`, `feat(v3-p4): cookie-banner auto-dismiss (D2)`, `feat(v3-p4): global privacy control (D5)`, `feat(v3-p4): privacy stats on home (C3)` (or sensible combinations).
